query check_event_times($name: String) {
    match {
        $a: Account { name: $name }
        $a has_event $e
        $e: AccountEvent
    }
    return {
        $a.name,
        $e.timestamp
    }
}
